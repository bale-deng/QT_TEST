Build (PowerShell):
- `mkdir build; cd build; cmake ..; cmake --build .`

Run app (PowerShell):
- `./bin/WeatherApp`

Run Python script directly (PowerShell):
- `python .\python\weather_service.py Beijing`

Notes:
- Ensure Qt6 and Python3 dev headers are installed so CMake can find them.