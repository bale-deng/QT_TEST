After changes:
- Configure/build with CMake (see suggested_commands.md).
- Run the app to verify UI starts.
- Optionally run the Python script standalone for quick validation.

No tests, linters, or formatters are defined in the repo.